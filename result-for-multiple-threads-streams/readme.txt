One thread has one stream.

For the speedup chart, refer to the char files in this dir.

For my PC GPU(rtx-8000), the nsys log files are in dir nsys-on-rtx-8000.
For the L20 GPU, do not have nsys logs, met errors when install nsys on L20.

